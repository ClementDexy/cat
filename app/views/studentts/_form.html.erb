<%= form_with(model: studentt, local: true) do |form| %>
  <% if studentt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(studentt.errors.count, "error") %> prohibited this studentt from being saved:</h2>

      <ul>
      <% studentt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :regno %>
    <%= form.number_field :regno %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :telephone %>
    <%= form.text_field :telephone %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
